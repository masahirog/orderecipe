<ol class="breadcrumb">
	<li><a href="/">ホーム</a></li>
	<li class="active">食材資材一覧</li>
</ol>


<div class="form-inline col-md-12" style="padding:10px;">
  <h3>食材資材の新規登録
    <a href="/materials/new">
      <span class="glyphicon glyphicon-plus-sign" aria-hidden="true">
      </span>
    </a>
  </h3>
</div>


<div class="col-xs-12 col-md-12">
  <%= form_tag Material, :method => 'get' do %>
    <div class="col-xs-3 col-md-2">
      <label>食材・資材名：</label>
      <%= text_field_tag :name, params[:name], class:"form-control" %>
    </div>
    <div class="col-xs-3 col-md-2">
      <label>発注名：</label>
      <%= text_field_tag :order_name, params[:order_name], class:"form-control" %>
    </div>
    <div class="col-xs-3 col-md-2">
      <label>仕入先：</label>
      <%= select_tag :vendor_id, options_from_collection_for_select(Vendor.all, :id, :company_name),
       include_blank: true, class: "form-control" %>
    </div>
    <div class="col-xs-3 col-md-2">
      <label>管理コード：</label>
      <%= text_field_tag :order_code, params[:order_code], class:"form-control" %>
    </div>
    <div class="col-xs-3 col-md-1">
      <label>終売：</label>
      <%= select :end_of_sales, :value, [["販売のみ", 0], ["終売のみ", 1]], {include_blank: true},class:"form-control" %>
    </div>
    <div class="kensaku form-group col-md-2">
      <label>　</label>
      　<%= submit_tag "検索" ,class:"btn btn-primary" %>
    </div>
  <% end %>
</div>
<div class="col-md-12">
  <%= paginate(@search) %>
</div>
<div>
  <div class="col-md-12" style="padding:10px;">
    <div class="col-md-2">食材・資材名</div>
    <div class="col-md-2">発注名</div>
    <div class="col-md-1">発注単位</div>
    <div class="col-md-1"></div>
    <div class="col-md-1">価格(円)</div>
    <div class="col-md-1">原価(円)</div>
    <div class="col-md-1">カテゴリ</div>
    <div class="col-md-1">仕入先</div>
    <div class="col-md-1">終売</div>
    <div class="col-md-1">使用</div>
  </div>
  <ul class='list-group' style="list-style-type: none;">
    <% @search.each do |material| %>
      <li class="li-active materials_li col-md-12 list-group-item"  style="cursor: pointer">
        <div class="material_name col-md-2"><%=link_to "#{material.name}", "/materials/#{material.id}/edit", method: :get %></div>
        <div class="col-md-2"><%= material.order_name %></div>
        <div class="col-md-1 text-right"><%= material.order_unit_quantity %><%= material.order_unit %></div>
        <div class="col-md-1 text-right"><%= number_with_delimiter(material.calculated_value) %><%= material.calculated_unit %></div>
        <div class="col-md-1 text-right"><%= number_with_delimiter(material.calculated_price) %></div>
        <div class="col-md-1 text-right"><%= material.cost_price.round(2) %></div>
        <div class="col-md-1"><%= material.category %></div>
        <div class="col-md-1"><%= material.vendor.company_name%></div>
        <div class="col-md-1 end_of_sales"><%= p "✔" if material.end_of_sales == 1 %></div>
        <div class="col-md-1">
          <%=button_to "確認", "/materials/include_material/#{material.id}", method: :get,class:"btn btn-primary" %>
        </div>
      </li>
    <% end %>
  </ul>
</div>

<% if @search.size == 0 then %>
<p>該当する食材・資材が存在しません</p>
<% end %>

<br>

  <%= paginate(@search) %>
  <!-- ページネーション -->
