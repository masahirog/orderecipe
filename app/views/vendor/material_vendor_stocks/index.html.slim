h2 = "#{@vendors[0].company_name} 様"

table.table
  thead
    tr
      th 商品名
      th = "#{@today} 在庫"
      th

  tbody
    - @materials.each do |material|
      tr
        td = link_to material.name,material_vendor_material_vendor_stocks_path(material_id:material.id)
        td = @hash[material.id][:latest_material_vendor_stock][0].end_day_stock if @hash[material.id][:latest_material_vendor_stock].present?
        td